node_modules
.DS_Store
# Worktrees: linked checkouts of this repo, carried by their own branches and
# never content of this one. The agent harness puts them under .claude/.
.worktrees
.claude/worktrees/
dist
coverage
*.local*
count.txt
.env
.env.development
.nitro
.tanstack
.output
.vitest-attachments
# Vitest browser-mode failure screenshots.
__screenshots__
*.swp
terraform.tfvars
# The infra dev-container profile's single-owner mount: an SSH private key
# the owner drops in for a remote host, never repository content.
.devcontainer/infra/local/
scratch
research
*.csv
# Transient cold-review output (light-review / assess-review); never committed.
review_findings.md
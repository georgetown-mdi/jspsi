[Unit]
Description=Revoke the psilink relay's lapsed exchange keys hourly

[Timer]
# A lapse is a whole number of days, so an hourly sweep revokes a row within an
# hour of its lapse.
OnCalendar=hourly
RandomizedDelaySec=5m
Persistent=true

[Install]
WantedBy=timers.target
